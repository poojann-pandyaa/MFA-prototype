# Core Deep Learning Framework
# Note: For CUDA-accelerated execution, install PyTorch matching your CUDA runtime, e.g.:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0

# Speaker Verification (ECAPA-TDNN)
speechbrain>=0.5.15
huggingface_hub>=0.19.0
transformers>=4.30.0

# Audio I/O & Preprocessing
soundfile>=0.12.0
scipy>=1.9.0
numpy>=1.22.0

# Model Configuration & Results Serialization
pyyaml>=6.0
pandas>=1.5.0
